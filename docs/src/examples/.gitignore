# Compiled C example binary.
qp.out

# Captured example output is generated by `make example_outputs` (a prerequisite
# of `make docs`) so it cannot go stale. qp.m.out is the exception: regenerating
# it needs a MATLAB licence, so it stays committed -- see matlab.rst.
qp.c.out
python/*.py.out
